# Полный сброс (удалить все и создать заново)
docker compose down -v
docker compose up

# После успешного создания - запустить в фоне
docker compose up -d

# Проверить PostgreSQL
docker exec -it moex_postgres psql -U admin -d moex_oltp
\dt

# Проверить ClickHouse
docker exec -it moex_clickhouse clickhouse-client --user admin --password admin123
USE moex_olap;
SHOW TABLES;

# Посмотреть логи если что-то не так
docker logs moex_postgres
docker logs moex_clickhouse